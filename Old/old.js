// let click = 1;
// let autoClicker = 0;
// let multiplier = 1;
// let toyShop = 0;
// const multiplierStartingCost = 100;
// const clickStartingCost = 20;
// const autoClickerStartingCost = 12;
// const toyShopStartingCost = 50;
// const toyFactoryStartingCost = 1000;

/*
function buyAutoClicker() {
  if (toys >= getAutoClickerCost()) {
    toys -= getAutoClickerCost();
    autoClicker++;
    update();
  }
}

function buyClick() {
  if (toys >= getClickCost()) {
    toys -= getClickCost();
    click++;
    update();
  }
}

function buyMultiplier() {
  if (toys >= getMultiplierCost()) {
    toys -= getMultiplierCost();
    multiplier++;
    update();
  }
}

function buyToyShop() {
  if (toys >= getToyShopCost()) {
    toys -= getToyShopCost();
    toyShop++;
    update();
  }
}

function getAutoClickerCost() {
  return ((autoClicker+1) * autoClickerStartingCost);
}

function getClickCost() {
  return ((click+1) * clickStartingCost);
}

function getMultiplierCost() {
  return ((multiplier+1) * multiplierStartingCost);
}

function getToyShopCost() {
  return ((toyShop+1) * toyShopStartingCost);
}
*/
